"use client";
// Removed local demo dialog CRUD, now empty, serves as placeholder for metrics or quick links.
// You can add summary cards, charts, or quick links to projects/tasks here for overview later.

export default function DashboardContent() {
  return (
    <div className="flex flex-col gap-12 items-center justify-center min-h-[40vh] py-20">
      {/* Example: Place to show quick stats in future */}
    </div>
  );
}